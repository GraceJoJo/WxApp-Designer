<!--pages/category/category.wxml-->
<!-- <text>pages/category/category.wxml</text> -->
<view wx:for="{{recordList}}" wx:key="urlPos">

  <view class="notify-item flex-wrp">
    <view class="avatar flex-item">
      <image src="{{item.image}}" />
    </view>
    <view class="notify-content flex-item">
      <text class="notify-title">{{item.productDescription}}</text>
      <text class="notify-source">{{item.description}}</text>
      <text class="notify-price">￥{{item.price}}</text>
    </view>
  </view>
</view>